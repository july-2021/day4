// c, java => need to specify the size()
// JS => Array is the dynamic array.
// JS => No need to specify the size
let list = [];

// Array of Numbers
let list1 = [1, 2, 3];

// Array of Strings
let list2 = ["A", "B", "C"];
let list3 = ["Delhi", "Calcutta", "Chennai", "Mumbai"];
